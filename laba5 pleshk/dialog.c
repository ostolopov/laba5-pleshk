#include "libs.h"

/*
void dialog(void)
{
    Ozon *products = NULL;
    int products_number = 0;
    int dialog_number = -1;
    int check;
    int db_load = 0;
    while (dialog_number != -2)
    {
        for (int i = 0; i < count_messages; i++)
        {
            printf("%s\n", messages[i]);
            if (db_load != 1 && i == 2)
                break;
        }
        printf("Выберите пункт:\n");
        check = get_int(&dialog_number, 0, 10);
        if (check == EOF)
            return;
        
        
    }
}

int choose_option (int dialog_number)
{
    
    return 0;
}

void exit_dialog (Ozon **product, int* products_number)
{
    if (*product == NULL)
    {
        printf("Работа программы завершена!\n");
        return;
    }
    else
    {
        free(product);
        printf("Работа программы завершена!\n");
        return;
    }
}
*/
